#include <stdio.h>

/**
 * main - Print the number of arguenments passed to it
 * @argc: Arguemnet count
 * @argv: Array of pointer to arguement strings
 * Return: 0
 **/

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
